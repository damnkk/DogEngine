{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "msvc build",
            "command": "cl.exe",
            "args": [
                "/Zi",
                "/EHsc",
                "/Fe:",
                "${fileDirname}//build/Debug/vulkan_learning.exe",
                "${file}",
				
            ],
            "problemMatcher": [
                "$msCompile"
            ],
            "group": "build"
        }
    ]
}
